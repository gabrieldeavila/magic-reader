import styled from "styled-components";

const SelectorWrapper = styled.div`
  position: fixed;
  background: var(--outline-0_5);
  border-radius: 5px;
`;

const SelectorSt = {
  Wrapper: SelectorWrapper,
};

export default SelectorSt;
